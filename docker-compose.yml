services:
  openapi-mcp-server:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    container_name: openapi-mcp-server
